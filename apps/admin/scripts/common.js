import {getLang,setLang,t,languageOptions} from '/shared/i18n/translations.js';
export const API=(window.BO_CONFIG?.API_BASE||localStorage.getItem('bo_api')||'/api').replace(/\/$/,'');
export const token=()=>localStorage.getItem('bo_token')||sessionStorage.getItem('bo_token');
export const user=(()=>{try{return JSON.parse(localStorage.getItem('bo_user')||sessionStorage.getItem('bo_user')||'null')}catch{return null}})();
export function init(){if(!user||user.role!=='admin'){location.href='login.html';return false}document.querySelectorAll('[data-lang]').forEach(e=>{e.innerHTML=languageOptions();e.onchange=x=>setLang(x.target.value)});document.querySelectorAll('[data-t]').forEach(e=>e.textContent=t(e.dataset.t));document.querySelectorAll('[data-logout]').forEach(e=>e.onclick=()=>{localStorage.removeItem('bo_token');localStorage.removeItem('bo_user');sessionStorage.removeItem('bo_token');sessionStorage.removeItem('bo_user');location.href='login.html'});return true}
export async function api(path,opt={}){const auth=token();const headers={...(opt.headers||{}),...(auth?{Authorization:`Bearer ${auth}`}:{})};if(opt.body!==undefined)headers['Content-Type']=headers['Content-Type']||'application/json';let r;try{r=await fetch(API+path,{...opt,headers})}catch(e){throw new Error('Unable to reach the server. Check your API connection.')}const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`Request failed (${r.status})`);return d}

export async function downloadAttachment(messageId,name='attachment'){const auth=token();const res=await fetch(API+'/messages/file/'+encodeURIComponent(messageId),{headers:{...(auth?{Authorization:`Bearer ${auth}`}:{})}});if(!res.ok){const d=await res.json().catch(()=>({}));throw new Error(d.error||'Could not download attachment')}const blob=await res.blob();const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=name||'attachment';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)}
