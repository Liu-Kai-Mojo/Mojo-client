# Local frontend test

Build with `npm run build`. Serve the repository with a static HTTP server rather than `file://`.

Before testing API calls against the local backend, set browser local storage key `bo_api` to `http://localhost:8787/api`.
